server:
  port: 8000
  servlet:
    context-path: /smart

spring:
  application:
    name: smart
  profiles:
    active: dev
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update
logging:
  file:
    name: logs.log

